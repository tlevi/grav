/*#include "physic.h"
#include "vector.h"


class qnode{
	friend class QTree;

	private:
		const vector2 center;
		const vector2 size;

		qnode<T>* children[4];
		phys* leaf;

		float mass;
};


class QTree{
	private:
		qtreenode * root;
		uInt size;

		const void Insert(const T& leaf, qnode& node);	


	public:
		const bool Insert(const T& leaf);
};
*/
